const name: string = 'Strider';

let hpPoints: number | string = 95;

hpPoints = 'FULL';

const isAlive: boolean = true;

console.log({
    name, hpPoints, isAlive
});



// Esta linea le dice a TS que es un modulo, para que no moleste
export {};